# 开发日志 — 2026-05-17

## 第八轮 — 动画重写 + 应用图标 + 配置模板 + 更新历史

### 动画重写（CSS 风格平滑过渡）

- [x] **架构重构：单帧游戏循环替代多路 DispatcherTimer**
  - 旧：3 个独立 DispatcherTimer（悬停/回归/弹跳各一个），状态机复杂
  - 新：`CompositionTarget.Rendering` 单帧循环驱动所有平滑过渡
  - 目标值/当前值架构：鼠标设置目标，每帧向目标值插值

- [x] **悬停→离开的平滑过渡**
  - 所有属性（缩放/倾斜/平移/阴影/辉光）同时向目标值插值
  - 类似 CSS `transition: all 300ms ease-out`
  - 悬停跟随速度 0.10，回归速度 0.06（返回更慢，更优雅）

- [x] **入场动画**
  - `Loaded` 事件触发弹入动画：0.82 → 1.0（BackEase, Amplitude=0.35, 480ms）
  - 卡片加载时从缩小状态弹入，类似 iOS SpringBoard 效果

- [x] **点击弹跳动画（SplineKeyFrame）**
  - 旧：手动 DispatcherTimer + 分段正弦函数
  - 新：WPF `DoubleAnimationUsingKeyFrames` + 4 个 `SplineDoubleKeyFrame`
  - 动画曲线：0ms 压入(0.88) → 140ms 过冲(1.07) → 270ms 回弹(0.97) → 420ms 静止(1.0)
  - 使用 `KeySpline` 自定义贝塞尔曲线，类似 CSS `@keyframes` + `cubic-bezier()`
  - Storyboard 独立于游戏循环（`_isBouncing` 标志隔离）

- [x] **视觉参数微调**
  - 最大倾斜角 3° → 5°（更明显的 3D 效果）
  - 悬停缩放 1.03 → 1.04
  - 阴影模糊 8→14（悬停）/ 12→14
  - 阴影深度 2→4

- [x] **被移除的代码**
  - `_returnTimer` / `_bounceTimer` DispatcherTimer 状态机
  - `OnReturnTick` / `OnBounceTick` / `BounceEase` / `CubicEaseOut`
  - `StopReturnTimer` / `_isAnimatingBack` / `_from*` 快照字段

### 应用图标

- [x] 将 `logo.png` 复制到 `Resources\logo.png`
- [x] csproj 添加 `<Resource Include="Resources\logo.png" />`
- [x] `MainWindow.xaml` 添加 `Icon="/Resources/logo.png"`
- [x] `ConfirmDeployWindow.xaml` 添加 `Icon="/Resources/logo.png"`

### 配置模板生成

- [x] `Templates\portable-apps.txt` — 便携应用清单模板
  - 格式：`#日期` / `#log:更新日志` / 直链 URL 每行一个
- [x] `Templates\version.txt` — 软件包日期文件

### 更新历史卡片

- [x] `MainViewModel.ChangelogText` — 从 devlog 汇总的用户友好更新日志
- [x] `MainWindow.xaml` 右侧面板新增 "📋 更新历史" 卡片
  - 位于版本检测卡片和进度显示卡片之间
  - ScrollViewer 内 TextBlock 绑定 ChangelogText
  - 最大高度 180px，超出自动滚动

### 涉及文件

| 文件 | 操作 |
|------|------|
| `Controls/AppCard.xaml.cs` | 重写（游戏循环 + SplineKeyFrame 弹跳 + 入场动画） |
| `Resources/logo.png` | 新建（应用图标） |
| `Templates/portable-apps.txt` | 新建（配置模板） |
| `Templates/version.txt` | 新建（版本日期模板） |
| `ViewModels/MainViewModel.cs` | 修改（新增 ChangelogText） |
| `Views/MainWindow.xaml` | 修改（图标 + 更新历史卡片） |
| `Views/ConfirmDeployWindow.xaml` | 修改（图标） |
| `LeafNeko.DeployTool.csproj` | 修改（Resource Include） |

### 构建结果
- dotnet build: 0 错误 0 警告
- dotnet publish: 单文件 EXE 成功

### 最新发布产物
`H:\develop\DNBZ\publish\LeafNeko.DeployTool.exe`

### 待上传仓库文件
- `Templates/portable-apps.txt` — 放到 Gitee 仓库 `portable-apps.txt`
- `Templates/version.txt` — 放到 Gitee 仓库 `version.txt`

---

## 第九轮 — 演示数据清理 + 独立进度窗 + 版本号 + 暗色模式 + 搜索 + 系统信息 + 部署历史 + 更新检测 + 自更新

### 1. 删除演示数据

- [x] 删除 `MainViewModel.LoadDemoData()` 方法及 8 个硬编码 AppItem
- [x] `LoadAppsAsync()` 失败时不再 fallback 到演示数据
- [x] 清单不可用时显示："暂无可用软件，请检查仓库配置"

### 2. 版本号系统

- [x] csproj 添加 `<Version>1.0.10</Version>`
- [x] MainViewModel 添加静态 `VersionText` 属性（从 Assembly 读取）
- [x] 顶部品牌栏显示版本号："LeafNeko 装机助手 v1.0.10"
- [x] 版本规则：仅递增小版本（1.0.10 → 1.0.11）

### 3. 一键部署独立进度窗

- [x] 新建 `Views/DeployProgressWindow.xaml` — 独立 Window，粉色圆角主题
- [x] 新建 `Views/DeployProgressWindow.xaml.cs` — INotifyPropertyChanged
- 窗口内容：
  - 📊 总进度条 + 已用时间 + 预计剩余
  - 每个任务独立卡片：名称、状态、阶段、总进度、下载进度、解压进度、速度
  - 取消按钮 → CancellationToken
- [x] `MainWindow.xaml.cs` `DeployAllBtn_Click` 改用独立窗口
  - 创建 DeployProgressWindow → Show → RunAsync → 完成后 Close

### 4. 暗色模式

- [x] 新建 `Themes/DarkTheme.xaml` — 暗色主题资源字典
  - 背景 `#1E1E2E`，卡片 `#2B2B3D`，文字 `#E8E8EC`
  - 粉色保留但调暗 `#E8919E`
- [x] `DeployConfig` 添加 `DarkMode` 属性（持久化到 config.json）
- [x] `App.xaml.cs` 添加 `ToggleTheme()` 静态方法（动态替换 ResourceDictionary）
- [x] 启动时根据 config 加载对应主题
- [x] `MainWindow` 顶部栏添加 `🌙/☀` 切换按钮

### 5. 软件搜索过滤

- [x] `MainViewModel` 添加 `SearchText` 属性
- [x] `ApplyFilter()` 同时匹配分类 + 关键字（Name + Category 模糊搜索）
- [x] `MainWindow.xaml` 分类标签栏右侧添加搜索框（🔍 + TextBox，实时过滤）

### 6. 系统信息面板

- [x] 新建 `Services/SystemInfoService.cs` — INotifyPropertyChanged
  - OS: `RuntimeInformation.OSDescription`
  - CPU: `Environment.ProcessorCount` 核心
  - RAM: `GlobalMemoryStatusEx` P/Invoke（可用/总计 GB）
  - C盘: `DriveInfo`（可用/总计 GB）
  - 磁盘 < 10GB 时 `DiskLow` 变红警告
- [x] 新建 `Converters/BoolToVisibilityConverter.cs`
- [x] `MainWindow` 右侧面板新增 "🖥 系统信息" 卡片（最上方）

### 7. 部署历史记录

- [x] 新建 `Models/DeployHistory.cs` — DeployHistoryEntry（时间/应用名/操作/结果）
- [x] 新建 `Services/HistoryService.cs` — JSON 读写 `history.json`
- [x] `MainViewModel` 添加 `RecentHistoryText`（最近 5 条）
- [x] `MainWindow` 右侧面板新增 "📝 部署历史" 卡片
- [x] 各部署方法完成后记录历史（便携/快捷方式/安装/一键部署全部）

### 8. 更新检测

- [x] `AppItem` 添加 `IsOutdated` 属性
- [x] `AppItemViewModel` 添加 `IsOutdated` / `HasUpdateIndicator`（已完成 + 过时）
- [x] `VersionService` 新增 `IsOutdated()` 静态方法（比较注册表版本 vs URL 中版本号）
- [x] `VersionService` 新增 `ExtractVersionFromUrl()` — 正则提取 URL 文件名中的版本号
- [x] `CheckVersions()` 增强：检测到已安装应用后设置 LocalVersion、IsOutdated
- [x] `AppCard.xaml` 添加橙色 "有更新" 指示器（圆点 + 文字）
- [x] 版本检测按钮改为 "🔄 检测更新"
- [x] 使用 source-generator regex（`[GeneratedRegex]`）

### 9. 自更新机制

- [x] `MainWindow.xaml.cs` 添加 `CheckSelfUpdateAsync()`
  - 启动时下载 `latest-version.txt`，与本地版本比较
  - 远端 > 本地 → MessageBox 提示 "发现新版本 vX.Y.Z"
- [x] `MainWindow.xaml.cs` 添加 `DownloadAndApplyUpdateAsync()`
  - 下载新版 EXE 到桌面临时目录
  - 下载完成后提示打开文件位置
- [x] 仓库命名规则：`LeafNeko.DeployTool_1.0.11.exe`

### 涉及文件

| 文件 | 操作 |
|------|------|
| `ViewModels/MainViewModel.cs` | 修改（删 LoadDemoData、版本号、搜索、历史、SystemInfo）|
| `ViewModels/AppItemViewModel.cs` | 修改（IsOutdated、LocalVersion 可写）|
| `Models/AppItem.cs` | 修改（IsOutdated 属性）|
| `Models/DeployConfig.cs` | 修改（DarkMode 属性）|
| `Models/DeployHistory.cs` | **新建**（部署历史记录模型）|
| `Services/SystemInfoService.cs` | **新建**（系统信息，含 P/Invoke）|
| `Services/HistoryService.cs` | **新建**（历史记录 JSON 读写）|
| `Services/VersionService.cs` | 重写（更新检测、版本比较、URL 版本提取）|
| `Views/DeployProgressWindow.xaml` | **新建**（独立部署进度窗口）|
| `Views/DeployProgressWindow.xaml.cs` | **新建**（进度窗口代码）|
| `Views/MainWindow.xaml` | 修改（搜索框、暗色切换、系统信息、历史卡片、版本号）|
| `Views/MainWindow.xaml.cs` | 修改（DeployAll 改独立窗口、自更新、历史记录）|
| `Themes/DarkTheme.xaml` | **新建**（暗色主题资源字典）|
| `App.xaml` | 修改（BoolToVisibilityConverter 注册）|
| `App.xaml.cs` | 修改（主题切换逻辑）|
| `Controls/AppCard.xaml` | 修改（有更新指示器）|
| `Converters/BoolToVisibilityConverter.cs` | **新建** |
| `LeafNeko.DeployTool.csproj` | 修改（`<Version>1.0.10</Version>`）|

### 构建结果
- dotnet build: 0 错误 0 警告
- dotnet publish: 单文件 EXE 成功 (63 MB)

### 最新发布产物
`H:\develop\DNBZ\publish\LeafNeko.DeployTool.exe`

### 待上传仓库文件
- `latest-version.txt` — 内容 `1.0.10`（初始版本号）

---

## 第十轮 — BUG修复 + 动画增强

### 动画与交互增强
- [x] 按钮按下缩放动画（ScaleTransform 0.97） + 悬停阴影辉光（PrimaryButton/SecondaryButton）
- [x] ShimmerProgressBar 流光动画样式（自定义 ControlTemplate + 动画 TranslateTransform 叠加层）
- [x] 侧边栏卡片入场动画（交错滑动 + 淡入，每张延迟 80ms）
- [x] 卡片入场动画（静态计数器交错 + BackEase 弹入）
- [x] 选中脉冲动画（SplineDoubleKeyFrame: 1.0→1.05→1.0, 350ms）
- [x] 分类切换平滑滚动到顶部（CompositionTarget.Rendering cubic ease-out）
- [x] 连接成功半透明叠加层动画（淡入→停留→淡出, 2500ms）

### BUG修复
- [x] 桌面目录从 `C:\Users\xxx\Desktop\LeafNeko.DeployTool` 改为 `%APPDATA%\LeafNeko.DeployTool`
- [x] DataContextChanged 事件订阅/取消订阅（防止内存泄漏 + 动画绑定失效）
- [x] 所有进度条样式统一为 ShimmerProgressBar

### 涉及文件
| 文件 | 操作 |
|------|------|
| `Themes/PinkTheme.xaml` | 修改（PrimaryButton/SecondaryButton/ShimmerProgressBar 样式）|
| `Themes/DarkTheme.xaml` | 修改（同上）|
| `Controls/AppCard.xaml` | 修改（Opacity="0" 入场初始）|
| `Controls/AppCard.xaml.cs` | 修改（入场动画、选中脉冲、DataContextChanged 订阅管理）|
| `Views/MainWindow.xaml` | 修改（x:Name 侧边栏卡片、ShimmerProgressBar、连接成功叠加层）|
| `Views/MainWindow.xaml.cs` | 修改（侧边栏入场动画、平滑滚动、连接动画、选中脉冲触发）|
| `Views/DeployProgressWindow.xaml` | 修改（ShimmerProgressBar 样式）|

### 构建结果
- dotnet build: 0 错误 0 警告

---

## 第十一轮 — BUG修复 + 状态栏替代弹窗 + 交互增强

### Part A — BUG修复

- [x] **A1 进度超100%**: DeployService/DeployProgressWindow/MainWindow 三处 OverallProgress 加 Math.Clamp
- [x] **A2 分类切换卡顿**: 新建 `ObservableRangeCollection<T>` 批量更新类，ApplyFilter 改用 `ClearAndAddRange()`
- [x] **A3 更新日志空白**: OnLoaded 开头调用 `_viewModel.InitChangelog()`
- [x] **A4 深色模式遗漏**: LicenseWindow.xaml 全部 11 处硬编码颜色替换为 StaticResource
- [x] **A5 确认窗口布局**: ConfirmDeployWindow 高度 620→700, 日志区域 160→120, 链接区域 80→120, 按钮间距增大
- [x] **A6 许可协议拖动**: LicenseWindow 最外层 Border 添加 MouseLeftButtonDown + DragMove handler
- [x] **A7 全选勾号右移**: 按钮文字 "☐ 全选"→"全选 ☐", "☑ 取消全选"→"取消全选 ☑"
- [x] **A8 删除部署历史卡片**: MainWindow.xaml 移除 HistoryCard
- [x] **A9 版本号 1.0.11**: csproj + latest-version.txt 更新; publish-and-upload.ps1 添加 EXE 重命名逻辑
- [x] **A10 卡片文字模糊修复**: AppCard 所有 TextBlock 添加 TextOptions.TextFormattingMode="Display" + TextRenderingMode="ClearType" + TextHintingMode="Animated" + UseLayoutRounding="True"

### Part B — 状态栏替代 MessageBox

- [x] **B1 删除连接覆盖层**: 移除 ConnectionSuccessOverlay + ShowConnectionSuccessAnimation 方法
- [x] **B2 新建 StatusBar 控件**: 6 状态（Hidden/Preparing/Ready/Working/Success/Error/PartialError），程序化动画
- [x] **B3 MainWindow 布局调整**: Grid 新增 Row 3（Auto）放 StatusBar，底部按钮移到 Row 4
- [x] **B4 替换 16 处 MessageBox**: 13 处替换为 StatusBar.Show()，保留 3 处 Yes/No 交互弹窗
- [x] **B5 一键部署确认遮罩**: 半透明背景 + 居中卡片（TaskCompletionSource<bool> 异步等待），BackEase 弹入动画
- [x] **声音反馈**: StatusBar 根据状态自动播放 SystemSounds（Asterisk/Hand/Exclamation）

### Part C — 新交互与动画增强

- [x] **C1 长按拖动多选**: DispatcherTimer 800ms + 静态 IsMultiSelectActive 标志 + MouseEnter 自动切换选中
- [x] **C2 全选 3D 波浪动画**: PlaySelectAllAnimation(int index, int total) — SkewX + TranslateY KeyFrame 动画，每张延迟 40ms
- [x] **C3 卡片 3D 参数增强**: MaxTiltAngle 5→8, MaxTranslate 3.5→6, HoverScale 1.04→1.05, 阴影 14→18/4→6, FollowSpeed 0.10→0.14, Skew 系数 0.3→0.5
- [x] **C4 失败重试按钮**: AppCard 状态 Error 时显示 "🔄 重试" 按钮，静态事件 + MainWindow 回调单应用重新下载安装
- [x] **C5 声音反馈**: 已集成到 StatusBar（Success→Asterisk, Error→Hand, PartialError→Exclamation）
- [x] **C6 下载测速 + ETA**: RepoService.MeasureSpeedAsync(), AppItemViewModel.EtaText/SpeedText, AppCard ETA 显示行

### 涉及文件
| 文件 | 操作 |
|------|------|
| `Views/MainWindow.xaml` | 修改（Grid 布局调整、StatusBar、确认遮罩、CardItemsControl 命名、删除覆盖层和历史卡片）|
| `Views/MainWindow.xaml.cs` | 重写（状态栏调用、确认遮罩逻辑、全选动画触发、重试回调、测速调用）|
| `ViewModels/MainViewModel.cs` | 修改（ObservableRangeCollection、ApplyFilter 优化、InitChangelog public）|
| `ViewModels/AppItemViewModel.cs` | 修改（SpeedText/EtaText/IsRetryVisible 属性）|
| `Controls/StatusBar.xaml` | **新建**（状态栏控件 UI）|
| `Controls/StatusBar.xaml.cs` | **新建**（6 状态逻辑 + 脉冲/闪烁/淡出动画 + 声音反馈）|
| `Controls/AppCard.xaml` | 修改（TextOptions 属性、UseLayoutRounding、ETA 行、重试按钮、MouseEnter）|
| `Controls/AppCard.xaml.cs` | 修改（长按多选、全选波浪动画、3D 参数调整、重试事件）|
| `Views/ConfirmDeployWindow.xaml` | 修改（高度/布局调整）|
| `Views/LicenseWindow.xaml` | 重写（11 处颜色主题化 + 拖动支持）|
| `Views/LicenseWindow.xaml.cs` | 修改（TitleBar_MouseLeftButtonDown handler）|
| `Views/DeployProgressWindow.xaml.cs` | 修改（OverallProgress Clamp）|
| `Services/DeployService.cs` | 修改（两处 OverallProgress Clamp）|
| `Services/RepoService.cs` | 修改（MeasureSpeedAsync 方法）|
| `Helpers/ObservableRangeCollection.cs` | **新建**（批量更新集合）|
| `LeafNeko.DeployTool.csproj` | 修改（Version 1.0.11）|
| `latest-version.txt` | 修改（1.0.11）|
| `publish-and-upload.ps1` | 修改（EXE 重命名逻辑）|

### 构建结果
- dotnet build: 0 错误 0 警告
