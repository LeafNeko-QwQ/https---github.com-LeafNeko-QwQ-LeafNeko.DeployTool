<Window x:Class="LeafNeko.DeployTool.Views.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="确认"
        Icon="/Resources/logo.png"
        Width="420" Height="240"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True"
        ResizeMode="NoResize"
        Background="Transparent"
        KeyDown="Window_KeyDown">

    <Border CornerRadius="16" Background="{StaticResource CardBackgroundBrush}"
            BorderBrush="{StaticResource PrimaryBrush}" BorderThickness="1"
            MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
        <Border.Effect>
            <DropShadowEffect Color="#1A000000" BlurRadius="20" ShadowDepth="4" Opacity="0.3"/>
        </Border.Effect>
        <Grid Margin="28">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 标题栏（可拖拽） -->
            <Border Grid.Row="0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown" Cursor="Hand"
                    Margin="0,0,0,12">
                <TextBlock x:Name="TitleText" Text="确认"
                           FontSize="16" FontWeight="Bold"
                           Foreground="{StaticResource TextPrimaryBrush}"/>
            </Border>

            <!-- 分割线 -->
            <Border Grid.Row="1" Height="1" Background="{StaticResource BorderLightBrush}"
                    Margin="0,0,0,16"/>

            <!-- 信息文本 -->
            <TextBlock x:Name="MessageText" Grid.Row="2"
                       FontSize="13" Foreground="{StaticResource TextSecondaryBrush}"
                       TextWrapping="Wrap" VerticalAlignment="Center"
                       TextOptions.TextFormattingMode="Display"
                       TextOptions.TextRenderingMode="ClearType"/>

            <!-- 按钮 -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button x:Name="CancelBtn"
                        Content="取消"
                        Width="100" Height="36"
                        FontSize="13"
                        Style="{StaticResource SecondaryButton}"
                        Margin="0,0,12,0"
                        Click="CancelBtn_Click"/>
                <Button x:Name="ConfirmBtn"
                        Width="100" Height="36"
                        FontSize="13"
                        Style="{StaticResource PrimaryButton}"
                        Click="ConfirmBtn_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
