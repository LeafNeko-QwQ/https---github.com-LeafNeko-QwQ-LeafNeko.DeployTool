<Window x:Class="LeafNeko.DeployTool.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:LeafNeko.DeployTool.Controls"
        xmlns:vm="clr-namespace:LeafNeko.DeployTool.ViewModels"
        Title="LeafNeko 装机助手"
        Icon="/Resources/logo.png"
        Width="1100" Height="750"
        MinWidth="900" MinHeight="600"
        WindowStartupLocation="CenterScreen"
        PreviewKeyDown="Window_PreviewKeyDown"
        Closing="Window_Closing"
        Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="56"/>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="64"/>
        </Grid.RowDefinitions>

        <!-- ========== 顶部品牌栏 ========== -->
        <Border Grid.Row="0" Background="{StaticResource CardBackgroundBrush}" BorderBrush="{StaticResource BorderLightBrush}" BorderThickness="0,0,0,1">
            <Grid Margin="24,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="⚙" FontSize="22" VerticalAlignment="Center"/>
                    <TextBlock FontSize="20" FontWeight="Bold"
                               Foreground="{StaticResource PrimaryBrush}"
                               Margin="8,0,0,0" VerticalAlignment="Center">
                        <Run Text="LeafNeko 装机助手"/>
                        <Run Text="{x:Static vm:MainViewModel.VersionText}"
                             FontSize="13" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </TextBlock>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <CheckBox IsChecked="{Binding AutoDarkMode}"
                              FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"
                              VerticalAlignment="Center" Margin="0,0,10,0"
                              Content="跟随系统"
                              ToolTip="自动跟随 Windows 系统主题"/>
                    <TextBlock Text="🌙" FontSize="14"
                               Cursor="Hand" Margin="0,0,12,0"
                               MouseLeftButtonDown="DarkModeToggle_Click"
                               ToolTip="切换深色/浅色模式"/>
                    <TextBlock Text="B站" FontSize="13"
                               Foreground="{StaticResource PrimaryBrush}"
                               Cursor="Hand" Margin="0,0,16,0"
                               MouseLeftButtonDown="BilibiliLink_Click"/>
                    <TextBlock Text="Gitee" FontSize="13"
                               Foreground="{StaticResource PrimaryBrush}"
                               Cursor="Hand"
                               MouseLeftButtonDown="GiteeLink_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ========== 分类标签栏 ========== -->
        <Border Grid.Row="1" Background="{StaticResource CardBackgroundBrush}" BorderBrush="{StaticResource BorderLightBrush}" BorderThickness="0,0,0,1">
            <Grid Margin="24,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <ItemsControl x:Name="CategoryTabs" VerticalAlignment="Center"
                              ItemsSource="{Binding Categories}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Border Padding="16,6" CornerRadius="16" Margin="0,0,2,0"
                                        Cursor="Hand"
                                        Background="Transparent"
                                        MouseLeftButtonDown="CategoryTab_Click"
                                        Tag="{Binding}"
                                        ContextMenuOpening="CategoryTab_ContextMenuOpening">
                                    <Border.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="📝 重命名" Click="CategoryRename_Click"/>
                                            <MenuItem Header="🗑 删除" Click="CategoryDelete_Click"/>
                                            <Separator/>
                                            <MenuItem Header="⬆ 上移" Click="CategoryMoveUp_Click"/>
                                            <MenuItem Header="⬇ 下移" Click="CategoryMoveDown_Click"/>
                                        </ContextMenu>
                                    </Border.ContextMenu>
                                    <StackPanel Orientation="Horizontal">
                                        <!-- 折叠箭头 -->
                                        <TextBlock Text="▼"
                                                   FontSize="10" Foreground="{StaticResource TextSecondaryBrush}"
                                                   VerticalAlignment="Center" Margin="0,0,4,0"
                                                   Name="collapseArrow"
                                                   Tag="{Binding}"
                                                   MouseLeftButtonDown="CategoryCollapseArrow_Click"/>
                                        <TextBlock Text="{Binding}"
                                                   FontSize="13" FontWeight="SemiBold"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   Name="tabText"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- 添加分类按钮 -->
                <Button Content="＋" Width="30" Height="30"
                        FontSize="14" FontWeight="Bold"
                        Foreground="{StaticResource TextSecondaryBrush}"
                        Background="Transparent" BorderThickness="0"
                        Cursor="Hand" VerticalAlignment="Center"
                        ToolTip="新建分类"
                        Click="AddCategoryBtn_Click"/>

                <!-- 搜索框（含历史下拉） -->
                <Border Grid.Column="1" CornerRadius="14" Background="{StaticResource CardAccentBrush}"
                        BorderBrush="Transparent" BorderThickness="1"
                        Padding="8,2" VerticalAlignment="Center" MinWidth="200">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🔍" FontSize="12" VerticalAlignment="Center" Margin="4,0,6,0"/>
                        <ComboBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                  ItemsSource="{Binding SearchHistory}"
                                  IsEditable="True"
                                  FontSize="12" Foreground="{StaticResource TextPrimaryBrush}"
                                  Background="Transparent" BorderThickness="0"
                                  VerticalAlignment="Center"
                                  Width="160"
                                  IsTextSearchEnabled="False"
                                  KeyDown="SearchBox_KeyDown"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- ========== 主内容区 ========== -->
        <Grid Grid.Row="2" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="320"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧：软件卡片网格 -->
            <Grid Grid.Column="0" Margin="24,0,12,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- 加载进度条（仅在加载时显示） -->
                <Border Grid.Row="0" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8"
                        Padding="12,10" Margin="0,0,0,8"
                        Visibility="{Binding OverallProgress, Converter={StaticResource ProgressToVisibilityConverter}}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                            <TextBlock Text="⏳" FontSize="14" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding ProgressStatus}"
                                       FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"
                                       Margin="6,0,0,0" VerticalAlignment="Center"
                                       TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                        <ProgressBar Height="6"
                                     Minimum="0" Maximum="100"
                                     Value="{Binding OverallProgress}"
                                     Foreground="{StaticResource PrimaryBrush}"
                                     Background="{StaticResource BorderLightBrush}"
                                     BorderThickness="0"
                                     Style="{StaticResource ShimmerProgressBar}"/>
                    </StackPanel>
                </Border>

                <ScrollViewer x:Name="CardScrollViewer" Grid.Row="1"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled"
                              ClipToBounds="False">
                    <ItemsControl x:Name="CardItemsControl"
                                  ItemsSource="{Binding FilteredApps}"
                                  Padding="4">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <controls:AppCard DataContext="{Binding}" Margin="0,0,16,16"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

            </Grid>

            <!-- 右侧：功能卡片区 -->
            <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" Margin="12,0,12,0">
            <StackPanel>

                <!-- 系统信息卡片 -->
                <Border x:Name="SystemInfoCard" Loaded="SidebarCard_Loaded"
                        Background="{StaticResource CardBackgroundBrush}" CornerRadius="12" Padding="16" Margin="0,0,0,12"
                        Effect="{StaticResource CardShadow}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <TextBlock Text="🖥" FontSize="20" VerticalAlignment="Center"/>
                            <TextBlock Text="系统信息" FontSize="14" FontWeight="SemiBold"
                                       Margin="6,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,3">
                            <Run Text="OS: "/><Run Text="{Binding SystemInfo.OsInfo}"/>
                        </TextBlock>
                        <TextBlock FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,3">
                            <Run Text="CPU: "/><Run Text="{Binding SystemInfo.CpuInfo}"/>
                        </TextBlock>
                        <TextBlock FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,3">
                            <Run Text="RAM: "/><Run Text="{Binding SystemInfo.RamInfo}"/>
                        </TextBlock>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock FontSize="11" Foreground="{StaticResource TextSecondaryBrush}">
                                <Run Text="C盘: "/><Run Text="{Binding SystemInfo.DiskInfo}"/>
                            </TextBlock>
                            <TextBlock Text=" ⚠ 空间不足" FontSize="11" Foreground="{StaticResource ErrorBrush}"
                                       Visibility="{Binding SystemInfo.DiskLow, Converter={StaticResource BoolToVisibilityConverter}}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- 便携应用部署卡片 -->
                <Border x:Name="PortableCard" Loaded="SidebarCard_Loaded"
                        Background="{StaticResource CardBackgroundBrush}" CornerRadius="12" Padding="20" Margin="0,0,0,16"
                        Effect="{StaticResource CardShadow}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📦" FontSize="24" VerticalAlignment="Center"/>
                            <TextBlock Text="便携应用部署" FontSize="15" FontWeight="SemiBold"
                                       Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Text="从云端下载免安装应用，解压到 C 盘根目录"
                                   FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"
                                   Margin="0,8,0,0" TextWrapping="Wrap"/>
                        <Button x:Name="DeployPortableBtn"
                                Content="执行部署"
                                Style="{StaticResource PrimaryButton}"
                                Margin="0,16,0,0"
                                Click="DeployPortableBtn_Click"/>
                    </StackPanel>
                </Border>

                <!-- 快捷方式部署卡片 -->
                <Border x:Name="ShortcutCard" Loaded="SidebarCard_Loaded"
                        Background="{StaticResource CardBackgroundBrush}" CornerRadius="12" Padding="20" Margin="0,0,0,16"
                        Effect="{StaticResource CardShadow}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🔗" FontSize="24" VerticalAlignment="Center"/>
                            <TextBlock Text="快捷方式部署" FontSize="15" FontWeight="SemiBold"
                                       Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Text="从云端下载快捷方式，自动复制到当前用户桌面"
                                   FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"
                                   Margin="0,8,0,0" TextWrapping="Wrap"/>
                        <Button x:Name="DeployShortcutsBtn"
                                Content="执行部署"
                                Style="{StaticResource PrimaryButton}"
                                Margin="0,16,0,0"
                                Click="DeployShortcutsBtn_Click"/>
                    </StackPanel>
                </Border>

                <!-- 版本检测卡片 -->
                <Border x:Name="VersionCheckCard" Loaded="SidebarCard_Loaded"
                        Background="{StaticResource CardBackgroundBrush}" CornerRadius="12" Padding="20" Margin="0,0,0,16"
                        Effect="{StaticResource CardShadow}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🔍" FontSize="24" VerticalAlignment="Center"/>
                            <TextBlock Text="版本检测" FontSize="15" FontWeight="SemiBold"
                                       Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Text="检测本地是否已安装清单中的软件"
                                   FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"
                                   Margin="0,8,0,0" TextWrapping="Wrap"/>
                        <Button x:Name="CheckVersionBtn"
                                Content="🔄 检测更新"
                                Style="{StaticResource SecondaryButton}"
                                Margin="0,12,0,0"
                                Click="CheckVersionBtn_Click"/>
                    </StackPanel>
                </Border>

                <!-- 更新历史卡片 -->
                <Border x:Name="ChangelogCard" Loaded="SidebarCard_Loaded"
                        Background="{StaticResource CardBackgroundBrush}" CornerRadius="12" Padding="20" Margin="0,0,0,16"
                        Effect="{StaticResource CardShadow}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <TextBlock Text="📋" FontSize="24" VerticalAlignment="Center"/>
                            <TextBlock Text="更新历史" FontSize="15" FontWeight="SemiBold"
                                       Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <ScrollViewer MaxHeight="180" VerticalScrollBarVisibility="Auto">
                            <TextBlock Text="{Binding ChangelogText}"
                                       FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"
                                       TextWrapping="Wrap" LineHeight="18"
                                       FontFamily="Segoe UI, Microsoft YaHei"/>
                        </ScrollViewer>
                    </StackPanel>
                </Border>

                <!-- 进度显示区 — 多任务动态列表 -->
                <Border x:Name="ProgressCard" Loaded="SidebarCard_Loaded"
                        Background="{StaticResource CardBackgroundBrush}" CornerRadius="12" Padding="20"
                        Effect="{StaticResource CardShadow}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <TextBlock Text="📊 进度显示"
                                       FontSize="15" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding ProgressStatus}"
                                       FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"
                                       Margin="12,0,0,0" VerticalAlignment="Center"
                                       TextTrimming="CharacterEllipsis"/>
                        </StackPanel>

                        <!-- 总进度（一键部署时显示） -->
                        <ProgressBar Grid.Row="0" Height="4"
                                     Minimum="0" Maximum="100"
                                     Value="{Binding OverallProgress}"
                                     Foreground="{StaticResource PrimaryBrush}"
                                     Background="{StaticResource BorderLightBrush}"
                                     BorderThickness="0"
                                     Margin="0,0,0,4"
                                     Style="{StaticResource ShimmerProgressBar}"
                                     Visibility="{Binding OverallProgress, Converter={StaticResource ProgressToVisibilityConverter}}"/>

                        <!-- 活跃任务列表 -->
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto"
                                      MaxHeight="300">
                            <ItemsControl ItemsSource="{Binding ActiveTasks}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border CornerRadius="8" Background="{StaticResource CardBackgroundBrush}"
                                                BorderBrush="{StaticResource BorderLightBrush}" BorderThickness="1"
                                                Padding="10" Margin="0,0,0,6">
                                            <StackPanel>
                                                <!-- 任务名称 + 状态 -->
                                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                                    <TextBlock Text="{Binding Name}"
                                                               FontSize="12" FontWeight="SemiBold"
                                                               Foreground="{StaticResource TextPrimaryBrush}"
                                                               TextTrimming="CharacterEllipsis"
                                                               MaxWidth="180"/>
                                                    <TextBlock Text="{Binding StatusText}"
                                                               FontSize="10" Foreground="{StaticResource PrimaryBrush}"
                                                               Margin="8,0,0,0" VerticalAlignment="Center"/>
                                                </StackPanel>

                                                <!-- 阶段文字 -->
                                                <TextBlock Text="{Binding PhaseText}"
                                                           FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"
                                                           Margin="0,0,0,4"
                                                           TextTrimming="CharacterEllipsis"
                                                           Visibility="{Binding PhaseText, Converter={StaticResource StringToVisibilityConverter}}"/>

                                                <!-- 总进度 -->
                                                <ProgressBar Height="4"
                                                             Minimum="0" Maximum="100"
                                                             Value="{Binding OverallProgress}"
                                                             Foreground="{StaticResource PrimaryBrush}"
                                                             Background="{StaticResource BorderLightBrush}"
                                                             BorderThickness="0"
                                                             Margin="0,0,0,4"
                                                             Style="{StaticResource ShimmerProgressBar}"
                                                             Visibility="{Binding OverallProgress, Converter={StaticResource ProgressToVisibilityConverter}}"/>

                                                <!-- 下载进度 -->
                                                <ProgressBar Height="3"
                                                             Minimum="0" Maximum="100"
                                                             Value="{Binding DownloadProgress}"
                                                             Foreground="{StaticResource PrimaryBrush}"
                                                             Background="{StaticResource BorderLightBrush}"
                                                             BorderThickness="0"
                                                             Margin="0,0,0,3"
                                                             Style="{StaticResource ShimmerProgressBar}"
                                                             Visibility="{Binding DownloadProgress, Converter={StaticResource ProgressToVisibilityConverter}}"/>

                                                <!-- 解压进度 -->
                                                <ProgressBar Height="3"
                                                             Minimum="0" Maximum="100"
                                                             Value="{Binding ExtractProgress}"
                                                             Foreground="{StaticResource PrimaryLightBrush}"
                                                             Background="{StaticResource BorderLightBrush}"
                                                             BorderThickness="0"
                                                             Margin="0,0,0,3"
                                                             Style="{StaticResource ShimmerProgressBar}"
                                                             Visibility="{Binding ExtractProgress, Converter={StaticResource ProgressToVisibilityConverter}}"/>

                                                <!-- 速度 -->
                                                <TextBlock Text="{Binding SpeedText}"
                                                           FontSize="10" Foreground="{StaticResource TextSecondaryBrush}"
                                                           Margin="0,2,0,0"
                                                           Visibility="{Binding SpeedText, Converter={StaticResource StringToVisibilityConverter}}"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>

            </StackPanel>
            </ScrollViewer>

            <!-- 一键部署确认遮罩 -->
            <Border x:Name="ConfirmOverlay" Panel.ZIndex="20"
                    Background="#80000000"
                    Opacity="0" Visibility="Collapsed"
                    MouseLeftButtonDown="ConfirmOverlayBg_Click">
                <Border x:Name="ConfirmCard"
                        CornerRadius="16"
                        Background="{StaticResource CardBackgroundBrush}"
                        BorderBrush="{StaticResource PrimaryBrush}" BorderThickness="1"
                        Width="420"
                        HorizontalAlignment="Center" VerticalAlignment="Center"
                        Effect="{StaticResource CardShadow}">
                    <Border.RenderTransform>
                        <ScaleTransform ScaleX="0.9" ScaleY="0.9"/>
                    </Border.RenderTransform>
                    <StackPanel Margin="32">
                        <TextBlock Text="🚀 一键部署全部"
                                   FontSize="20" FontWeight="Bold"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   HorizontalAlignment="Center" Margin="0,0,0,8"/>
                        <TextBlock Text="将依次执行以下操作："
                                   FontSize="13" Foreground="{StaticResource TextSecondaryBrush}"
                                   Margin="0,0,0,12"/>
                        <Border CornerRadius="8" Background="{StaticResource CardAccentBrush}" Padding="16" Margin="0,0,0,20">
                            <StackPanel>
                                <TextBlock Text="📦 便携应用 — 下载并部署到 C:\"
                                           FontSize="12" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,6"/>
                                <TextBlock Text="🔗 快捷方式 — 复制到桌面"
                                           FontSize="12" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,6"/>
                                <TextBlock Text="⬇ 全部安装版软件 — 逐个下载安装"
                                           FontSize="12" Foreground="{StaticResource TextPrimaryBrush}"/>
                            </StackPanel>
                        </Border>
                        <TextBlock x:Name="ConfirmWarningText"
                                   FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"
                                   TextWrapping="Wrap" Margin="0,0,0,16"
                                   TextAlignment="Center"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Button Content="取消"
                                    Width="110" Height="38"
                                    FontSize="13"
                                    Style="{StaticResource SecondaryButton}"
                                    Margin="0,0,16,0"
                                    Click="ConfirmCancelBtn_Click"/>
                            <Button Content="✅ 确认部署"
                                    Width="130" Height="38"
                                    FontSize="13"
                                    Style="{StaticResource PrimaryButton}"
                                    Click="ConfirmStartBtn_Click"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Border>
        </Grid>

        <!-- ========== 状态栏 ========== -->
        <controls:StatusBar x:Name="StatusBar" Grid.Row="3" Margin="24,4"/>

        <!-- ========== 底部操作栏 ========== -->
        <Border Grid.Row="4" Background="{StaticResource CardBackgroundBrush}" BorderBrush="{StaticResource BorderLightBrush}" BorderThickness="0,1,0,0">
            <Grid Margin="24,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="SelectAllBtn"
                            Content="全选 ☐"
                            Style="{StaticResource SecondaryButton}"
                            Margin="0,0,8,0"
                            Click="SelectAllBtn_Click"/>
                    <Button x:Name="DeployAllBtn"
                            Content="🚀 一键部署全部"
                            Style="{StaticResource PrimaryButton}"
                            Margin="8,0,12,0"
                            Click="DeployAllBtn_Click"/>
                    <TextBlock Text="{Binding SelectedCountText}"
                               FontSize="13"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button x:Name="ExportBtn"
                            Content="📄 导出报告"
                            Style="{StaticResource SecondaryButton}"
                            FontSize="12"
                            Margin="0,0,10,0"
                            Click="ExportBtn_Click"
                            ToolTip="导出部署历史为 TXT/HTML"/>
                    <Button x:Name="InstallSelectedBtn"
                            Content="⬇ 一键安装已选"
                            Style="{StaticResource PrimaryButton}"
                            VerticalAlignment="Center"
                            Click="InstallSelectedBtn_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
